[project]
name = "tenderai-pythonservice"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "minio>=7.2.20",
    "pika>=1.3.2",
    "pymupdf>=1.26.7",
]
